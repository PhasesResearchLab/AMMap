{
  "build": { "dockerfile": "./Dockerfile" },
  "customizations": {
    "codespaces": {
      "openFiles": [
        "examples/README.md"
      ]
    },
    "vscode": {
      "extensions": [
        "nimsaem.nimvscode",
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/opt/conda/bin/python"
      }
    }
  },
  "hostRequirements": {
      "cpus": 4,
      "memory": "16gb",
      "storage": "16gb"
  },
  "onCreateCommand": "git submodule update --init --recursive && nim c --d:release --threads:on --app:lib --out:nimplex.so nimplex/src/nimplex.nim && nim c --d:release --threads:on --app:lib --out:utils/plotting.so nimplex/src/nimplex/utils/plotting.nim"
}
